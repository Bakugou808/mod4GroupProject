import React, { Component } from 'react'

export default class Comments extends Component {
    render() {
        return (
            <div>
                {/* Hello I'm a post: we need a button for likes, we need a caption displayed,
                 and we need form to add comments, --> need to display likes and comments */}
                 display comments, add comments, like comments
            </div>
        )
    }
}
